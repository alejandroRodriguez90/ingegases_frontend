// EN: config-global.js
console.log("Cargando config-global.js...");

window.AppConfig = {
    ubicacionesDisponibles: ["Bogotá", "Cali"]
    // Aquí podrías añadir otras constantes globales en el futuro,
    // como roles de usuario, URLs de API, etc.
};